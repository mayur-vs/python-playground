number_list = [3222, 422, 1, 100, 22122]

maximum_number = number_list[0]

for each_number in number_list :
    if each_number > maximum_number :
        maximum_number = each_number

print(f"Maximum number in the number list is {maximum_number}")